# Copy to .env in the project root (docker compose reads it automatically).
# testsrc = lab pattern, still writes real mp4 into ./sidecar/media
STREAM_LONG=testsrc
STREAM_WIDE=testsrc
STREAM_IR=testsrc

# Live turret examples:
# STREAM_LONG=rtsp://panoptes-base.local:554/stream1
# STREAM_IR=http://panoptes.local/thermal/stream
